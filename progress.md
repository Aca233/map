Original prompt: 将政治页面改成接近 HOI4 的布局，左侧为领袖与三排槽位，点击槽位后在右侧打开选择界面；第一排为征兵法案、贸易法案、4 个政治顾问，第二排为坦克/舰艇/飞机/军需品装备/工业集团/理论家，第三排为陆军部长/海军部长/空军部长/3 个总司令。

- 2026-03-12: 继续中断后的政治页工作，确认仓库已有 `political-panel`、HOI4 UI 贴图和 `GameStateManager` 选择逻辑。
- 2026-03-12: 现状问题是政治页仍是三段网格加弹出式小列表，不符合用户要求的“左侧槽位 + 右侧整块选择面板”。
- 2026-03-12: 数据层可复用，需新增 4 个政治顾问独立槽位、3 个高指挥独立槽位，并把 `industry_corporation` 池按关键词拆成“军需品装备制造商”和“工业集团”。
- 2026-03-12: 补齐右侧选择区默认关闭逻辑：`index.html` 的选择侧栏现在带 `id="ui-selector-panel"` 且初始 `hidden`，`main.ts` 在每次打开政治页前会调用 `collapsePoliticalSelectorPanel()` 重置右侧状态。
- 2026-03-12: 待验证交互：政治页初始不显示右侧选择区；点击任意槽位后显示；再次点击同一槽位后收起；关闭并重新打开政治页时右侧保持收起。
- 2026-03-12: 根据新反馈，政治总面板默认宽度已缩到左栏尺寸，只有右侧选择区打开时才加上 `selector-open` 展开到双栏，避免右半边出现空背景。
- 2026-03-12: 继续收紧默认态外形：政治总面板默认改为 `height: auto` + `max-height`，只在 `selector-open` 时恢复全高，避免右侧收起后底部保留大块空背景。
- 2026-03-12: 按最新反馈继续收口政治页默认态：总面板改为 `top: 64px` 固定停靠，避开顶部旗帜/菜单组合区，不再与上方 UI 重合。
- 2026-03-12: 左侧默认政治界面取消主列滚动条，改为压缩头像区、焦点按钮与三排槽位高度，让默认态更接近 HOI4 的一屏布局；右侧选择区保留滚动能力，长列表继续在选择列内滚动。
- 2026-03-12: 三排槽位再次统一了卡片高度、图框高度、标题/值区高度、组内 padding 与列间距，减少默认态像弹窗/网页卡片墙的感觉。
- 2026-03-12: `npm run build` 再次通过；这轮主要剩浏览器里的肉眼微调，如果用户还觉得偏高或偏松，优先继续压缩领袖区与组标题高度。
- 2026-03-12: 修正工业五槽候选池：不再把全局/别国工业机构混入当前国家的可选列表，改为仅使用当前国家显式机构池和初始已挂载机构。
- 2026-03-12: 取消非法律槽位的自动预填；默认只展示国家初始政治数据里实际已挂载的法案/机构，其余槽位保持空缺，更接近 HOI4 初始政治界面。
- 2026-03-12: 上述修正后 `npm run build` 通过；如果后续还要继续提高准确性，可再补一层“idea allow/available 触发器”解析，而不是只靠 TAG 前缀和初始历史数据。
- 2026-03-12: 修正法国等国家的“旧 token / 新 token”混用问题：开局 `add_ideas` 中像 `francois_darlan`、`philippe_fequant` 这类无前缀旧 token 现在会自动归一到 `FRA_...` 角色 taxonomy 上，用于恢复默认已挂载顾问/军种部长显示。
- 2026-03-12: 为未进入 taxonomy 的老式工业 idea 增加兜底分类（如 `famh`），避免当前国家明明有开局工业集团却被显示成空槽。
- 2026-03-12: 仍未并入 MIO/design_team 作为工业槽候选来源；法国这类国家剩余空槽若要更接近 HOI4 默认态，需要把 `mio:FRA_..._organization` 这条数据链接到政治页槽位。
- 2026-03-12: 将 `starting_politics.json` 中的 `design_team` / `mio:` 组织接入政治页工业槽。当前实现会递归提取开局设计团队，按装备 `type` 优先分类到坦克 / 舰艇 / 飞机 / 军需 / 工业集团槽，再并入候选池与默认显示。
- 2026-03-12: 补强 token 显示与归一：`mio:` 前缀会被规范化；组织 token 会尝试回退到同国旧本地化键（如 `FRA_hotchkiss`、`renault_fra`），避免 UI 直接显示原始 `_organization` token。
- 2026-03-12: 法国这类国家现在能从开局 `design_team` 中恢复雷诺 / ACF 等默认工业槽内容；仍然没有对应开局设计团队的数据槽位会继续保持空缺。
- 2026-03-13: 为法国工业机构补了一层显式国家目录回退，避免仅凭导出的 taxonomy / design_team 导致舰艇、飞机、军需、工业集团候选过少；当前法国 5 个工业槽会优先使用这套目录生成候选。
- 2026-03-13: 法国 MIO token 增加归一和显示别名，如 `FRA_renault_organization -> renault_fra`，并为 `ACF / FCM / 布列塔尼造船厂 / 卢瓦尔造船厂 / 布洛克 / 阿米奥 / 莱维塞尔 / 索玛 / 圣艾蒂安兵工厂 / 施耐德 / 法兰西无线电公司` 提供本地中文显示回退。
- 2026-03-13: 工业槽默认不再因为开局装备 `design_team` 被自动占用；设计团队现在主要作为候选来源，左侧默认槽位保持更接近 HOI4 的未任命状态。
- 2026-03-13: 右侧法国工业候选卡片补充了更具体的专业说明，如“轻型飞机设计商 / 护航舰造船厂 / 火炮制造商 / 电子集团”，减少通用占位文案感。
- 2026-03-13: 当前用户新需求改为“为游戏加入游戏中的图片文件”，已把顶部工具栏按钮切到 HOI4 原始按钮精灵图的左右半区状态显示，不再直接整张缩放。
- 2026-03-13: 已确认本机 HOI4 安装目录为 `C:/Program Files (x86)/Steam/steamapps/common/Hearts of Iron IV`，并新增 `scripts/export_hoi4_flags.py` 将 `gfx/flags/*.tga` 导出为前端可用的 `public/assets/flags/hoi4/*.png`。
- 2026-03-13: 顶部玩家国旗现改为优先使用导出的 HOI4 真实国旗文件，并按国家当前意识形态选择对应旗面（如 `FRA_democratic.png`、`GER_fascism.png`）；先前的手工 SVG 旗面已移除。
- 2026-03-13: 新增 `scripts/export_hoi4_spirit_icons.mjs`，现已按 `common/ideas/*.txt` 的 `picture = ...` 与 `.gfx` 里的 `texturefile` 关联导出国家精神图标到 `public/assets/hoi4_ui/gfx/interface/ideas_exported/`，当前 `224` 个开局精神中已有 `223` 个拿到真实游戏图标，剩余 `ETH_christmas_offensive_idea` 在本机 HOI4 目录下未检到对应贴图，前端会继续走已有回退图标。
- 2026-03-13: `GameStateManager` 的国家精神 UI 现会优先读取 `public/assets/data/spirit_icon_map.json` 使用游戏内真实图标，并优先显示游戏本地化描述；真实图标不再叠渐变遮罩，也移除了 hover 上移，避免“瞄上去平移”和“图案被挡住”。
- 2026-03-13: 新增 `scripts/export_hoi4_character_icons.mjs`，按 `common/characters/*.txt` 的 `portraits.small/large` 与 `.gfx` 的 `texturefile` 关系导出人物候选图标到 `public/assets/hoi4_ui/gfx/interface/character_icons_exported/`，并生成 `public/assets/data/character_icon_map.json`。当前全局开局可招募角色 `5121` 个 token 中已有 `4667` 个映射到图标，法国当前候选组已达到 `69/69` 全覆盖。
- 2026-03-13: 右侧选择面板的人物候选项与左侧已任命人物槽位，现都会优先使用游戏内角色图标；原先通用 `select_advisor.png` 空框和图面遮罩只保留给无真实图标的回退场景，避免“这一排都没有图标”。
- 2026-03-13: `character_icon_map.json` 已扩展为携带 `ideaToken / traits / description`；`GameStateManager` 现已补齐对应 TS 类型，并继续保持“候选项描述只取游戏内文本”的逻辑：先读角色导出描述，再读本地化 `*_desc/_tooltip/_long`，不再回退固定模板文案。
- 2026-03-13: 修正“瞄上去没有描述”的根因：选择面板候选按钮此前用了原生 `disabled`，导致非可选项拿不到 hover/focus 提示。现在改为 `aria-disabled` 保留悬浮能力，并复用全局 `#tooltip` 显示游戏内标题与描述；`npm run build` 已再次通过（仅保留既有静态资源运行时解析 warning），本轮未使用浏览器验证。
- 2026-03-13: `export_hoi4_spirit_icons.mjs` 已扩展为同时导出普通 `idea` 与 `*_organization` 的游戏内图标，重新生成后的 `public/assets/data/spirit_icon_map.json` 现覆盖 `6013` 个 idea token 与 `447` 个组织 token，去重后导出真实图标 `1603` 张；像 `volunteer_only`、`FRA_acf_organization`、`renault_fra` 这类此前容易回退成固定图的 token 现已命中真实贴图。
- 2026-03-13: 国家精神说明已去掉按类别拼接的固定模板描述，`renderNationalSpirits()` 现只显示游戏内文本；同时选择界面的 law / idea / MIO 组织图标也会优先读取导出的游戏图标映射，再退回现有占位图。`npm run build` 已通过，本轮仍未使用浏览器验证。
- 2026-03-13: 用户反馈“国家精神的描述都没了”后，确认根因是 `public/assets/data/localization.json` 在旧导出逻辑下没有把 idea / character 的 `_desc / _tooltip / _long / _name` 变体一并收进来。现已在 `scripts/convert-hoi4-data.mjs` 增加 `addLocalizationKeyVariants()`，并重新执行数据转换；法国开局国家精神如 `FRA_victors_of_wwi`、`FRA_disjointed_government`、`FRA_protected_by_the_maginot_line` 的游戏原文描述已重新进入本地化文件，`npm run build` 通过，本轮仍未使用浏览器验证。
- 2026-03-13: 用户反馈“最上面的国家精神弹框会被标题挡住”后，已将国家精神 tooltip 从局部 `::after` 伪元素切到和选择项相同的全局 `#tooltip` 浮层，避免被政治面板标题区或父容器裁切；`index.html` 中伪元素样式现仅对显式 `data-tooltip` 生效，避免出现空弹框。`npm run build` 已通过，本轮仍未使用浏览器验证。
- 2026-03-13: 用户继续反馈“民族精神 tooltip 动一下就消失了”后，已把国家精神 tooltip 改成稳定的共享交互态：hover 时只在离开按钮/说明框区域后延迟收起，click 时会锁定当前精神说明，再次点击同一精神或点击空白处关闭；同时移除民族精神遗留的 `data-tooltip::after` 伪元素样式，避免旧实现干扰。`npm run build` 已通过，本轮仍未使用浏览器验证。
- 2026-03-13: 用户继续反馈法国工业/舰艇候选仍有“没有图标”的项后，确认根因是前端法国回退目录用了旧别名 token（如 `FRA_bretagne_shipyard_organization` / `FRA_loire_shipyard_organization` / `FRA_compagnie_francaise_de_radio_organization`），而 HOI4 实际 token 分别是 `FRA_aeb_organization`、`FRA_acl_organization`、`sfr`；同时 `famh` / `sfr` 不在现有 taxonomy，导致旧导出脚本未把它们写入 `spirit_icon_map.json`。现已修正 `GameStateManager` 的法国别名/目录，并扩展 `export_hoi4_spirit_icons.mjs` 以额外收集 common ideas 中的真实 idea token；重导后 `famh -> idea_famh.png`、`sfr -> idea_generic_radio_concern.png`、`FRA_aeb_organization -> fra_aeb.png`、`FRA_acl_organization -> fra_acl.png` 均已命中游戏图标。本轮仍未使用浏览器验证。
- 2026-03-13: 用户继续反馈“《伦敦海军条约》签署国图标有问题，圣艾蒂安兵工厂没有图标”后，核对 HOI4 原文件确认：`MTG_naval_treaty_adherent` 已正确指向游戏内专用贴图 `idea_mtg_naval_treaty_adherent.png`，不是模板图；而“圣艾蒂安兵工厂”的真实组织 token 是 `FRA_mas_organization`，此前前端误用了旧别名 `FRA_saint_etienne_arsenal_organization`，导致无法命中 `idea_mas.png`。现已在 `GameStateManager` 中把法国别名、中文名覆盖、军需候选目录统一切到 `FRA_mas_organization`，并保留旧 token 到新 token 的兼容别名。`npm run build` 已通过，本轮仍未使用浏览器验证。
- 2026-03-13: 针对用户最新反馈“图标还是有问题，还有还是鼠标动一下描述就没了”，本轮继续收口两处。1) `scripts/export_hoi4_spirit_icons.mjs` 的 `.dds -> .png` 现优先调用本机 Pillow 转换，再回退 JS 解码；已删除并重导 `idea_mtg_naval_treaty_adherent*.png`，本地文件检查确认 `《伦敦海军条约》签署国` 图标已恢复为正常船锚图。2) 民族精神 tooltip 现改成“只认民族精神按钮本身”的锚点逻辑：hover 与 click 共用同一个大框样式，但 tooltip 自身不再接收鼠标，也不再参与 hover 判定；隐藏只在移出民族精神按钮后触发，不再因为鼠标经过 tooltip 或轻微移动就消失。另将 tooltip 面板默认宽度调宽，并保留游戏描述换行。`npm run build` 已再次通过，本轮仍未使用浏览器验证。
- 2026-03-13: 根据用户新增反馈“顶上按钮太小、政治面板 UI 与背景不居中、顶上的状态栏也要用游戏里的文件”，本轮只改 `index.html`，未使用浏览器验证。顶部栏现直接套用 HOI4 `topbar/background.png`，8 个主按钮改为使用 `toolbar/*.png` 的双态整张按钮精灵，不再是小号网页按钮；资源条改成 `resource_item_bg.png` 外框，并把政治点、稳定度、战争支持度、人力、燃料、指挥点、陆海空经验等图标统一切到游戏文件（包括 `pol_power_icon.png`、`stability_icon.png`、`war_support_icon.png`、`manpower_icon.png`、`command_power_icon.png`、`armyoverview_button.png`、`navyoverview_button.png`、`airoverview_button.png` 等）。时间区也改挂 `topbar_date_overlay.png`、`ingameclock_bg.png` 和 `speed_step.png`。
- 2026-03-13: 政治面板默认态重新居中到背景中：`#political-panel` 顶部避让高度已按新顶栏抬到 `88px`，背景改为 `center top / 100% 100%`，默认态 `.political-content` 采用居中布局，`.political-main-column` 改为 `width: min(560px, 100%) + margin: 0 auto`；仅在 `selector-open` 时再切回左主列 `536px` + 右侧 `524px` 选择列的双栏布局，减少默认态“左挤右空”的问题。`npm run build` 已通过，本轮仍未使用浏览器验证。
- 2026-03-13: 用户继续反馈“顶上 UI 有问题”后，本轮继续只改 `index.html` 顶栏样式，未使用浏览器验证。修正了资源条里 `span.resource-icon.sprite-icon` 没有盒模型导致工厂/陆海空经验图标容易塌掉或显示异常的问题；顶部栏改为按 `background_extended.png` + 深色底层填充摆放，避免下半段透明区把顶栏显得发空；国旗框尺寸对齐 `large_flag_frame.png` 原图比例，主按钮排与右上时间区重新按素材尺寸定位；世界紧张度也切到 `world_tension_icon.png` + `resource_item_bg.png` 的游戏内样式，与资源条保持统一。
- 2026-03-13: 针对用户继续反馈“还是有点歪，而且多了黑色背景”，继续只改 `index.html`，未使用浏览器验证。确认 `topbar/background_extended.png` 实际只有前 `81px` 高度带不透明像素，底部 `6px` 是透明带；因此顶栏容器现裁到 `81px` 并去掉额外 `box-shadow`，避免透明底边再叠出一层发黑背景。同时把国旗、主按钮排、资源条、右上时间区整体各上提约 `1px`，继续收紧顶栏基线对齐。`npm run build` 已再次通过。
- 2026-03-13: 用户继续反馈“国旗歪了，顶上 UI 左边还有一点没靠边”后，继续只做本地代码修正，未使用浏览器验证。`#flag-area` 已整体左上再顶 `1px`，`background_extended.png` 也同步向左偏移 `1px`，抹掉最左侧残留缝隙；国旗内部显示区从手写近似 inset 改成精确 `82x52`，与导出的 HOI4 旗面原图尺寸一致；`major_country_flag_overlay.png` 不再被拉伸到整面旗，而是按原始 `43x29` 只贴左上角，避免金色遮罩把旗面压歪。同时 `GameStateManager` 里的真实国旗贴图改为 `100% 100%` 完整铺入，不再使用 `cover` 裁切。`npm run build` 已通过。
- 2026-03-13: 根据用户最新截图继续收口左上角顶栏。重新检查 `topbar/background_extended.png` 左上裁片后确认：背景图最左 4 列只有半透明抗锯齿，不左移足够像素就会露出页面底色；同时该背景素材本身已经带了完整的国旗槽位外框，我之前再叠 `large_flag_frame.png` / `major_country_flag_overlay.png` 属于重复上框，正是“看着歪”的主因。现已把顶栏背景改为向左偏移 `4px`，并把 `#flag-area` 缩回背景自带槽位的内尺寸 `82x52`，移除额外旗框覆盖层，只保留真实国旗贴面和底部 TAG 标记，避免双层边框错位。`npm run build` 已通过。
- 2026-03-13: 用户继续反馈“国旗歪了”后，进一步按背景自带旗槽的实际宽度微调。基于 `background_extended.png` 左上裁片的黑色内槽估算，当前真实国旗 `82px` 宽但槽位可视宽度约 `89px`，之前属于贴左摆放，所以右侧会残留一条更明显的黑边。现将 `#flag-area` 的水平位置从 `left: 4px` 调到 `left: 8px`，把国旗居中回背景槽位里；本轮只动这一项，避免再次把顶栏整体带歪。`npm run build` 已通过。
- 2026-03-13: 用户再次反馈“还是歪了”后，重新按左上背景槽位的真实内框做纵向定位。放大裁片确认：真正的黑色旗槽从大约 `y=13px` 才开始，之前 `#flag-area` 放在 `top: 4px` 实际是把国旗顶进了上方金属边框，因此视觉上一直像“国旗歪了”。现只把 `#flag-area` 的 `top` 调整为 `13px`，让旗面回到黑色内槽里；左右位置保持上一轮已收好的居中值不动。`npm run build` 已通过。
- 2026-03-13: 用户继续反馈“还差一点，还有没有过渡感”后，继续只收左上国旗，不动其它顶栏。基于前面测得的黑色内槽约 `89x55`、真实旗面 `82x52`，这轮把国旗再向右下微调到更接近内槽中心（`left: 12px`、`top: 14px`），避免仍然偏向左上；同时给旗面补了一层很轻的高光 + 内阴影渐变，只做与黑色槽位的过渡，不再恢复会拉歪旗面的装饰框。`npm run build` 已通过。
- 2026-03-13: 用户继续反馈“还是歪了”后，复核内槽中心坐标，确认上一轮把国旗水平位置向右推过头了：`top: 14px` 基本贴近内槽垂直中心，但 `left: 12px` 会比中心偏右约 5px。现只把 `#flag-area` 的水平位置回调到 `left: 7px`，保持上一轮新增的轻微高光和内阴影不动，避免再次把纵向对齐带歪。`npm run build` 已通过。
- 2026-03-13: 用户继续反馈“还有一点点黑边”后，确认这轮更像是过渡层本身造成的视觉黑边，而不是国旗位置又偏了。上一轮 `::after` 为了补过渡感加了一层左右向暗边渐变，这会在红白蓝边缘外再压出一圈发黑边。现保留顶部轻高光与底部内阴影，只移除左右向暗边渐变，尽量保住槽位过渡感，同时消掉残留黑边。`npm run build` 已通过。
- 2026-03-13: 用户继续反馈“框大了，国旗小了”后，确认问题已从对齐转为尺寸不足。现将 `#flag-area` 从保守的 `82x52`/微调坐标恢复到背景黑色内槽的实测尺寸 `89x55`，并同步回到与槽位匹配的 `left: 8px`、`top: 13px`，让真实国旗重新填满内槽，不再留下整圈黑底。`npm run build` 已通过。
- 2026-03-13: 用户继续反馈“国旗再向左移动一点，还有国旗在边框下面”后，继续只收左上国旗。现将 `#flag-area` 再向左回调 `1px` 到 `left: 7px`；同时不再缩整个容器，而是只把内部旗面 `flag-face` 向内收 `1px`，并强化 `::after` 的顶部高光 / 边缘内阴影，让真实旗面看起来压在金属边框下面，而不是直接铺在边框同一层上。`npm run build` 已通过。
- 2026-03-13: 用户继续反馈“还是对不上”后，回退掉上一轮过度模拟“压在边框下面”的处理。确认更稳的做法仍然是让旗面直接填满背景黑色内槽本身，而不是再人为内缩一圈做假边框；因此本轮将 `#flag-area` 水平位置回到 `left: 8px`，`flag-face` 改回 `inset: 0` 完整铺满，同时 `::after` 只保留顶部轻高光与底部内阴影，去掉会制造假边界的侧边/整圈阴影。`npm run build` 已通过。
- 2026-03-13: 用户继续反馈“还是对不上啊”后，改用本地素材合成而不是继续凭截图猜像素。按当前 CSS 参数把 `background_extended.png` 与 `FRA_democratic.png` 在本地直接合成后确认：背景左上黑色内槽在容器坐标中真实范围是 `x=4..92, y=13..67`，而当前 `#flag-area` 放在 `left: 8px`，整面旗会右偏整整 `4px`。现只将 `#flag-area` 回调到 `left: 4px`，其余尺寸与上下位置保持不动。`npm run build` 已通过。
- 2026-03-13: 用户继续反馈“背景的框和国旗大小不匹配”后，继续只做本地素材比对和 CSS 微调，未使用浏览器。将 `background_extended.png`、`FRA_democratic.png` 与 `large_flag_frame.png` 本地合成后确认：更接近游戏视觉的不是“旗面直接铺满黑槽”，而是“独立外框 92x58 + 内缩旗面 88x54 + 很轻的高光/底部暗角”。现已把 `#flag-area` 改为 `left: 2px; top: 11px; width: 92px; height: 58px;`，`flag-face` 改为 `inset: 2px`，并用 `large_flag_frame.png` 作为前景框叠到旗面之上，收掉“框大旗小”和边缘过硬的问题。待用户继续按实机观感确认是否还要再动 1px 级别位置。
- 2026-03-13: 用户继续反馈“感觉上下有点空”并提供截图后，继续只微调国旗内部铺满度，不动外框。现已将 `flag-face` 与其过渡层 `::after` 的内缩从四边 `2px` 调整为 `上下 1px / 左右 2px`，让旗面在保持左右边框比例不变的前提下，进一步吃掉顶部和底部黑底空隙。
- 2026-03-13: 用户继续反馈“按钮少了，还有鼠标瞄上去的高亮有延迟”后，继续只改顶栏按钮本身，未使用浏览器。确认当前 HTML 只挂了 8 个按钮，而素材目录里还存在 `international_market_button.png`、`intelligence_button.png`、`ledger_button.png` 三个未接入按钮；现已把它们补进 `#main-menu-bar`。同时将 `.menu-btn` 的过渡从 `background-position 0.12s steps(1, end)` 改为只对 `filter / transform` 做短过渡，避免 hover 时高亮状态要等一拍才切换。
- 2026-03-13: 用户最新要求“政治面板也是用游戏中的”后，继续只做本地代码替换，不使用浏览器。`index.html` 里的政治页现将主底图、标题条、关闭按钮、信息区块、意识形态条、分组标题、焦点按钮、槽位卡片与右侧选择列尽量切到 HOI4 原始贴图驱动：标题改用 `header_bg.png` + `close_button_small.png`，信息区/分组容器改用 `generic_bg_307x113.png` / `idea_group_header.png`，意识形态区改用 `pol_piechart_bg.png` + `pol_piechart_overlay_63x63.png`，焦点按钮改为 `add_national_goal_button.png` 三帧贴图并配 `ongoing_focus_goal_icon.png`，右侧头图改为 `subview_header_bg_375x101.png`，当前选择条改为 `button_359x34.png`，槽位卡片改为 `idea_entry_bg_4.png` / `idea_entry_bg_3.png`，领袖区也收掉手搓渐变改为游戏内通用面板贴图。
- 2026-03-13: 本轮继续收口政治页里残留的纯 CSS 小部件，不改结构和交互，只把领袖特质圆章、国家精神角标、右侧候选状态条和槽位徽章切到 `generic_text_circle.png` / `generic_text_bg_88.png` 等 HOI4 自带小框贴图。
- 2026-03-13: 未使用浏览器，已在当前代码基础上重新执行 `npm run build`，构建通过；仍仅保留 Vite 对运行时静态资源路径的既有提示和 chunk 体积 warning。
- 2026-03-13: 用户继续要求“按原有贴图大小重新布局 UI”后，开始按原图宽高重排政治页骨架：主面板按 `pol_view_bg.png` 的 `550px` 宽度收回，右侧选择列按 `pol_idea_bg.png` 的 `514px` 宽度对齐，右侧标题/当前选择条分别按 `375x101` 和 `359x34` 居中，焦点按钮按 `259x83` 回归原比例。
- 2026-03-13: 三排 6 槽位不再拿 `462x60` 长条背景硬拉成高卡片，而是改成 `462px` 组宽内的 `72px * 6 + 6px * 5` 网格，并以 `63x63` 的原生槽位框作为核心视觉尺寸；领袖区与信息区也同步收回到 `148px / 307px / 362px` 这组贴图宽度关系。
- 2026-03-13: 本轮仍未使用浏览器，只做本地尺寸核对和 `npm run build` 验证；构建通过。如果用户下一步还觉得哪里不对，优先继续按这套固定尺寸微调 padding / gap / 对齐，而不要再把贴图整体拉伸放大。
- 2026-03-13: 按用户要求回退到“政治面板也是用游戏中的”之前的状态，已撤回该指令之后对政治页的贴图化和原图尺寸强绑定改动，恢复为上一阶段的自定义深色布局；仅回退政治页相关样式，未触碰顶部条等其它已调好的部分。`npm run build` 已通过。
- 2026-03-13: 用户反馈“打开选择界面的动画有问题，还有选择界面鼠标瞄上去就不要有弹窗了”后，右侧选择列已从 `hidden/display:none` 的硬切换改为 `width/opacity/transform` 过渡展开；同时移除了选择列表项的 hover/focus tooltip 绑定，仅保留国家精神等原本需要的提示。`npm run build` 已通过。
- 2026-03-13: 用户继续反馈“有些图标用错了”后，已直接对照本机 HOI4 原版 `interface/countrypoliticsview.gfx` 修正政治槽位默认贴图映射：法律槽默认回到 `add_pol_idea_button`，政治顾问 / 理论家 / 陆海空军部长 / 高指挥回到原版 `officer_corp/select_advisor`，不再误用 `idea_political_tab` / `idea_military_tab` / `idea_technology_tab` 这类分类页签图；工业 5 槽仍保留对应 manufacturer / concern 原图。`npm run build` 已通过，本轮仍未使用浏览器验证。
- 2026-03-13: 用户继续指出“右下角是有个小图标的”后，确认 `select_advisor.png` 原图本身就自带右下角小角标，之前是左侧槽位渲染里追加的中文 `politics-slot-badge` 把这块压住了。现已改为仅在完全没有槽位贴图时才显示文字 badge，保留原版空槽贴图的完整角标与细节。`npm run build` 已通过，本轮仍未使用浏览器验证。
- 2026-03-13: 用户继续反馈“肖像框有空位”后，确认空位来自 `leader-section` 默认 `align-items: stretch`：右侧国家信息列更高时会把左侧 `leader-portrait-container` 整列一起拉长，导致名字下面出现多余黑色空区。现已将 `leader-section` 改为 `align-items: flex-start`，并把 `leader-portrait-container` 固定为 `flex: 0 0 148px` + `align-self: flex-start`，使肖像列按内容自然收高。`npm run build` 已通过，本轮仍未使用浏览器验证。
- 2026-03-13: 用户继续反馈“还是没有右下角小图标”后，重新对照本机 HOI4 原版 `interface/countryofficercorpview.gui` / `.gfx` 确认缺的不是 `select_advisor.png` 本体，而是单独叠在顾问类槽位右下角的 `GFX_chief_and_theorist_strip`。现已将 `gfx/interface/officer_corp/chief_and_theorist_strip.dds` 导出为 `public/assets/hoi4_ui/gfx/interface/officer_corp/chief_and_theorist_strip.png`，并在 `GameStateManager` 里按原版 frame 映射恢复政治顾问/理论家/陆海空军部长/高指挥槽位的右下角类别角标；只在没有原生头像、使用回退底图时显示，不覆盖真实人物头像。`npm run build` 已通过，本轮仍未使用浏览器验证。
- 2026-03-13: 用户继续指出“这里空了位置，有点难看”后，确认是 `leader-section` 右列比左侧肖像列更高，导致肖像下方露出一块纯背景。现将左侧改成 `leader-sidebar` 纵向栏，把“选择国家焦点”按钮移入肖像下方并设为可拉伸填充剩余高度，避免这块留白，同时不再把肖像卡内部强行拉高。`npm run build` 已通过，本轮仍未使用浏览器验证。
- 2026-03-13: 用户继续反馈“党派下面还有点空”后，确认主要来自 `.ideology-section` 之前设了偏大的 `min-height` 与上下 padding，右侧文字列也只是自然流排版，导致“民主主义阵营 / 民主主义 / 上次选举”下面还会留一截空区。现已将该区块收紧为贴近圆环图高度，并让 `.party-info` 改为纵向居中排布，压掉底部空白。`npm run build` 已通过，本轮仍未使用浏览器验证。
- 2026-03-13: 用户继续要求“底部相同，高度相同”后，不再单独压某一列内容，而是把 `.leader-section` 从 `align-items: flex-start` 改为 `align-items: stretch`，让左侧 `leader-sidebar` 与右侧国家信息列按同一总高度拉齐；左侧多出来的高度仍由焦点按钮吸收，因此两列底边会一致，肖像本体不会被再次拉出空白。`npm run build` 已通过，本轮仍未使用浏览器验证。
- 2026-03-13: 用户继续要求“让党派视图与国家焦点一个底线”后，补了一层运行时高度同步，而不再只靠 CSS 近似拉齐。`GameStateManager` 现会在政治数据刷新后、`main.ts` 也会在政治面板打开和窗口 resize 时，读取 `.ideology-section` 的实际高度并同步给 `.leader-focus-tree-btn`，使党派视图与国家焦点按钮始终按同一高度和底线显示。`npm run build` 已通过，本轮仍未使用浏览器验证。
- 2026-03-13: 用户继续反馈“右下角有空位”后，确认空白主要来自党派区右侧没有承载任何内容。现已在 `index.html` 给 `.ideology-section` 追加 `#ui-ideology-breakdown` 区域，并在 `GameStateManager.renderIdeologyChart()` 中把 `set_popularities` 渲染为右侧支持度明细行（含颜色填充和百分比），用已有意识形态数据把右下角空位补满，而不是继续保留纯黑底。`npm run build` 已通过，本轮仍未使用浏览器验证。
- 2026-03-13: 用户继续反馈“还是有空位”并指出国家精神区右下角仍像少一格后，确认问题改为第二排国家精神仅有 3 枚图标时默认左对齐，视觉上会在右下留下一个明显空槽。现将 `.spirit-list` 改为 `justify-content: space-between`，让第二排不足 4 枚时也铺开整行，减弱右下角空位感。`npm run build` 已通过，本轮仍未使用浏览器验证。
- 2026-03-13: 用户要求“撤回该更改，我指的是外面的空白区域”后，已撤回国家精神 `justify-content: space-between` 的改动。进一步确认外层空白主要来自 `.political-main-column` 作为 `political-content` 的 flex 子项被默认 `stretch` 到整块高度，内容不够时底部就会露出一大片空背景。现已给 `.political-main-column` 增加 `align-self: flex-start`，让主列按内容高度自然收起，不再被强行拉满整个政治面板内容区。`npm run build` 已通过，本轮仍未使用浏览器验证。
- 2026-03-13: 用户继续要求“对齐，选择国家焦点右边下半部分有空白”后，改为按右列真实总高度同步左侧焦点按钮，而不是继续让右列 stretch。现将 `.leader-section` / `.leader-sidebar` 恢复为 `flex-start`，并把 `syncPoliticalOverviewLayout()` 的逻辑改成读取 `.country-info-container` 与 `.leader-portrait-container` 的实际高度，再反推出 `.leader-focus-tree-btn` 应有高度，使左侧肖像+焦点按钮整列与右侧“国家精神+党派视图”整列底线对齐，不再在焦点按钮右侧留下下半块空白。`npm run build` 已通过，本轮仍未使用浏览器验证。
- 2026-03-13: 用户继续指出“领导人肖像内有 TAG、右下角有图案，政治面板与顶部衔接不上”后，已一并收口：移除了肖像内部左下角的 `ui-leader-portrait` TAG 角标和右下角的 `ui-leader-traits` 圆形意识形态图案，避免与顶部国旗区信息重复；同时将政治面板 `top` / `height` 从 `88px` 改为与顶栏实际高度一致的 `81px`，消掉政治面板与顶部条之间的缝隙。`npm run build` 已通过，本轮仍未使用浏览器验证。
- 2026-03-13: 用户继续要求“打开选择面板不要改变右边的政治面板”后，已把选择面板从原先的“撑宽整个政治面板并压缩主列”改成桌面端右侧独立浮层。当前 `#political-panel.selector-open` 不再扩宽主面板；`.political-selector-panel` 改为绝对定位在政治面板右侧外缘并仅做显隐/位移动画，原政治面板宽度和主列布局保持不变。移动端 `@media (max-width: 1180px)` 仍保留原先纵向堆叠逻辑，避免窄屏浮层跑出视口。`npm run build` 已通过，本轮仍未使用浏览器验证。
- 2026-03-13: 用户继续指出“国旗上还有个国家TAG”后，已移除左上国旗内部的 `player-flag-label` 文本层，并同步删掉 `GameStateManager.updateCountryBadge()` 中对国旗 TAG 文本的回写逻辑，避免后续再次渲染出国家缩写。当前国旗仅保留旗面和外框，不再叠字。`npm run build` 已通过，本轮仍未使用浏览器验证。
